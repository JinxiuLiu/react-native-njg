import React, { Component } from 'react';
import { Text } from 'react-native';

export default class Release extends Component {
	constructor(props){
      super(props)
  }
  render() {
    return (
      <Text>Hello Release!</Text>
    );
  }
}